# CKAD Exam Resources

## Setup environment

raw github link `source <(curl "https://raw.githubusercontent.com/chdalski/ckad/refs/heads/main/env.sh")`

or per short-url:

- use an url shortener like [tinyurl](https://tinyurl.com/) and create a shorturl to your raw env
- call with `source <(curl -L "https://tinyurl.com/ckadenv")`  
